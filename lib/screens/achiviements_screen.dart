import 'package:flutter/material.dart';

class AchiviementsScreen extends StatelessWidget {
  const AchiviementsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('A'),
    );
  }
}
